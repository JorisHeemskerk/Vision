Disection van de lists.mat
deze lijst uitgelezen met scipy
de lijst bleek 9 indexes te hebben, na wat onderzoek en vergelijk met de list_English_Img.m heb ik deze conclusies getrokken over de data op de indexes:
# 0 == ALLnames
# 1 == ???????? starts with 1 ends on 0, len of 1 per image (appears to be a weird extra thing, no clue, we have everything we need)
# 2 == ALLlabels
# 3 == classlabels
# 4 == classnames
# 5 == NUMclasses
# 6 == TSTind
# 7 == VALind
# 8 == TRNind == 9 == TXNind

methodes bleken te lastig